# CLAUDE.md

<!-- placeholder. `python3 System/scripts/build_views.py` overwrites this file -->

Entry point for agents working in this vault. Identical in content to `AGENTS.md`.

This file is generated from two places and holds no rules of its own:

- `Maps & Manuals/Me.md`, the card about the person who owns this vault.
- `System/Agent Rules.md`, the rules shared by every installation.

Edit whichever of those owns the rule, then run `python3 System/scripts/build_views.py`.
If you are reading this sentence in a working vault, the build has not run yet.
