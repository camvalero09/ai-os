# CLAUDE.md

> **If you are the owner of this vault, this file is not for you.** It is the instruction sheet the AI reads at the start of every session, and it needs nothing from you. Nothing here is a task. The files worth your attention are `Maps & Manuals/Me.md`, which is about you, and `Maps & Manuals/Active Context.md`, which is what is happening now.

Entry point for AI agents working in this vault. This file only redirects; instructions live in the vault, where every fact has exactly one home.

## Read these two files before any task, in this order

1. `Maps & Manuals/Me.md`, permanent rules: who the user is, how to work with them, what to avoid.
2. `Maps & Manuals/Active Context.md`, current priorities, active efforts, open decisions, and task routing.

This vault already has 36 tested workflows and tools, listed in `Maps & Manuals/Skill Map.md`. Look there before deciding how to do something, rather than inventing an approach. That the list exists is the one thing you cannot work out from the folders.

## Consult when the task needs it

- `Maps & Manuals/Vault Map.md`, folder structure; read before creating or moving files.

## Conventions that apply to every task

Full list in Me.md and Vault Map. The three most violated: no em-dashes in any output the user will read; YAML frontmatter on all Atlas and Effort notes; wikilinks always carry the full vault-relative path (never bare `[[Note Name]]`).

This file is a redirect and holds no rules of its own. It changes only when the reading path itself changes, and then through the structural change protocol in Me.md, with `AGENTS.md` updated to match exactly. Any new rule belongs in the vault note that owns it, never here.
